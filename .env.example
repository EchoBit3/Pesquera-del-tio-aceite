# Copia este archivo como .env y ajusta los valores al entorno local.
# Este archivo SI se versiona; .env NO se versiona (ver .gitignore).

# URL base del servicio REST provisto por json-server
VITE_API_URL=http://localhost:3001/desembarques
